<%- include("layout") %>
<h1>Welcome to Members Only</h1>

<% if (messages.length === 0) { %>
  <p>No messages yet.</p>
<% } else { %>
  <% messages.forEach(msg => { %>
    <h3><%= msg.title %></h3>
    <p><%= msg.text %></p>
  <% }) %>
<% } %>
